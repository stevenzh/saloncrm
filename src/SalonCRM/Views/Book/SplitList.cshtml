@Html.DevExpress().GridView(
    settings =>
    {
        settings.Name = "gvSplit";
        settings.CallbackRouteValues = new { Controller = "Book", Action = "SplitList", BookProjectID = ViewData["BookProjectID"] };
        settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);
        settings.KeyFieldName = "SplitID";

        settings.Columns.Add(column =>
        {
            column.Caption = "工种";
            column.FieldName = "Position";
            column.EditorProperties().ComboBox(p =>
            {
                p.TextField = "Text";
                p.ValueField = "Value";
                p.ValueType = typeof(string);
                p.DataSource = ViewBag.Position;
            });
        });
        settings.Columns.Add(column =>
        {
            column.FieldName = "UserID";
            column.Caption = "员工";
            column.EditorProperties().ComboBox(p =>
            {
                p.TextField = "UserCnName";
                p.ValueField = "Id";
                p.ValueType = typeof(string);
                p.DataSource = ViewBag.Salesman;
            });
        });
        settings.Columns.Add("Percentage", "分配比");
        settings.Columns.Add("Amount", "分配金额");
        settings.Columns.Add("HandicraftFee", "手工费");

        settings.Settings.ShowHeaderFilterButton = false;
        settings.Settings.ShowFilterRow = false;
        settings.Settings.ShowFilterRowMenu = false;
        settings.SettingsPager.PageSize = 20;


        settings.CommandColumn.Visible = true;
        settings.CommandColumn.Width = 120;
        settings.CommandColumn.ShowNewButtonInHeader = true;
        settings.CommandColumn.ShowNewButton = true;
        settings.CommandColumn.ShowDeleteButton = true;
        settings.CommandColumn.ShowEditButton = true;

        settings.SettingsEditing.AddNewRowRouteValues = new { Controller = "Book", Action = "AddNewSplitPartial", BookProjectID = ViewData["BookProjectID"] };
        settings.SettingsEditing.UpdateRowRouteValues = new { Controller = "Book", Action = "UpdateSplitPartial", BookProjectID = ViewData["BookProjectID"] };
        settings.SettingsEditing.DeleteRowRouteValues = new { Controller = "Book", Action = "DeleteSplitPartial", BookProjectID = ViewData["BookProjectID"] };

    }).Bind(Model).GetHtml()